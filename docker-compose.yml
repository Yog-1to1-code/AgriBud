version: '3.8'

services:
  agribud:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agribud_app
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    restart: unless-stopped
